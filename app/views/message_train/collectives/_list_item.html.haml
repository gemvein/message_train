- if box.present?
  %li{ html_options }
    = link_to box.title, message_train.collective_box_path(collective_path_part(box.parent), box.division)
    - if unread_count > 0
      = badge unread_count.to_s