= turbo_frame_tag "message_#{message.id}_#{kind}_toggle" do
  - if collective.nil?
    = button_to icon(icon), message_train.box_conversation_path(@box.division, @conversation, objects: { 'messages' => {message.id.to_s => message.id.to_s}}, mark_to_set: mark_to_set), options
  - else
    = button_to icon(icon), message_train.collective_box_conversation_path(collective.path_part, @box.division, @conversation, objects: { 'messages' => {message.id.to_s => message.id.to_s}}, mark_to_set: mark_to_set), options
